{
  "description": "Read Fleet lane and setup state at session start without launching work or changing settings.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/fleet-session-hook.mjs\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
